namespace Game.Shared.Constants.EntityID {
    public enum InteractType {
        Default,
        Traverse,           // ladders, cliff
        InfoPopup,          // Units SpeechBubble; Items / Interactables Infos
        Toggle,             // Doors open/close
    }
}