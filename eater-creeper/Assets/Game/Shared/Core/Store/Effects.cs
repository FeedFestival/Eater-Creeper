using Game.Shared.Constants.Store;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.Shared.Core.Store {
    internal static class Effects {

        internal static void Init() {
            //Store.AddEffect(StoreAction.SetPlayerState, (GameState store, PlayerState playerState) => {

            //    GameplayState gameplayState;
            //    if (playerState == PlayerState.BrowsingMenu) {
            //        gameplayState = GameplayState.Loading;
            //    } else {
            //        gameplayState = GameplayState.BrowsingMenus;
            //    }

            //    Store.Dispatch(StoreAction.SetGameplayAndPlayerState, (gameplayState, playerState));
            //});
        }
    }
}